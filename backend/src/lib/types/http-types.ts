

/**
 * all types related to http must be located here
 * 
 */

// those are datatypes those we use as http verbs
export type HTTP_VERBS = 'GET' | 'POST' | 'POST' | 'DELETE';


// those are status codes we use for http frequently
// you can add more when needed
export type STATUS_CODE = 404 | 200 | 503 | 100;

